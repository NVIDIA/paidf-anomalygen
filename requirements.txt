# --- ML training & model utilities ---
accelerate==1.14.0
diffusers==0.39.0
einops==0.8.2
peft==0.20.0
safetensors==0.8.0
transformers==5.14.1

# --- Vision & detection ---
fvcore==0.1.5.post20221221
pycocotools==2.0.11
scikit-image==0.26.0
timm==1.0.28
# Pinned to the tip of upstream main.
git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4

# --- Image / video I/O ---
imageio==2.37.4
opencv-python-headless==4.14.0.94
pillow==12.3.0

# --- Data & HuggingFace ---
datasets==5.0.1
huggingface-hub==1.26.0
obstore==0.11.0
pandas==3.0.5
webdataset==1.0.2

# --- Text / tokenization ---
ftfy==6.3.1
qwen-vl-utils==0.0.14
sentencepiece==0.2.2

# --- Stats & visualization ---
matplotlib==3.11.1
scikit-learn==1.9.0

# --- Config / CLI / serialization ---
attrs==26.1.0
cattrs==26.1.0
hydra-core==1.3.4
omegaconf==2.3.1
protobuf==7.35.1
pydantic==2.13.4
pyyaml==6.0.3
tyro==1.0.15

# --- Logging & runtime ---
loguru==0.7.3
requests==2.34.2
termcolor==3.3.0
tqdm==4.70.0

# --- GPU monitoring & CUDA helpers ---
nvidia-cudnn-frontend==1.26.0
nvidia-ml-py==13.610.43

# --- Required for cosmos-anomalygen ---
megatron-core==0.18.2

# --- Required for cosmos-framework runtime / inference ---
better-profanity==0.7.0
boto3==1.43.59                   # cosmos3._src.vfm.utils.vlm.pretrained_models_downloader
multi-storage-client==1.0.1      # cosmos_framework/scripts/inference.py:15
nltk==3.10.0
pytz==2026.3.post1               # cosmos_framework/callbacks/heart_beat.py:7
retinaface-py==0.0.2
# Held back: 0.28.2 drops wandb.util.generate_id, which cosmos-framework calls to start a run.
wandb==0.28.1

# --- Required for auto mask placement GUI ---
flask==3.1.3
# Not imported any more: the GUI serves its own UI and the page calls /api with a relative base, so
# every request is same-origin and needs no CORS header. Kept pinned because it is already attested
# in LICENSE-3rd-party.txt, and regenerating that to drop one unused package would re-open the
# third-party licence review for no functional gain. Drop it from both files together.
flask-cors==6.0.5

# --- Transitive pins (nothing here is imported directly) ---
jaraco.context==6.1.2
msgpack==1.2.1
onnxscript==0.7.1                # transformer-engine-torch, runtime
wheel==0.48.0
setuptools==84.0.0

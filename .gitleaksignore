# Triaged gitleaks findings, by fingerprint (<commit>:<path>:<rule>:<line>).
# Everything not listed here must fail the secret-scan job. A fingerprint pins commit, file, rule
# and line, so an entry cannot widen to cover a new leak in the same file.
# Only add one after inspecting the value without printing it, and rotating if it needed rotating.

# False positive: `curl -s -u "${SONAR_TOKEN}:"` — the rule matched the -u syntax, but the argument
# is a shell variable, so no value was ever committed.
56cf1ce00058b43d8e674b10f40fcd4b79659704:.gitlab-ci.yml:curl-auth-user:76

# Expired: an S3 presigned URL inherited from the upstream Cosmos README, with X-Amz-Expires=300
# dated 2025-06-11. An AKIA access key *id* is an identifier, not a secret. Gone at HEAD.
# Two fingerprints, one occurrence: gitleaks reports it raw and percent-decoded.
a112adf6eed74e0aaffd59a9c578161a06bb07db:README.md:aws-access-token:56
